"""
Политика исполнения запросов к данным через MCP.

Зафиксировано для исполнителя БД (промпт) и для разработчиков: любой доступ к PostgreSQL
в этом приложении выполняется только через MCP-слой (stdio server-postgres), а не через
прямые драйверы в коде приложения. Изменение/удаление данных вне read-only MCP не
предусмотрено текущим сервером; при появлении мутаций их следует выносить в отдельные
MCP-инструменты с явной политикой, а не расширять произвольный SQL из модели.
"""

MCP_EXECUTION_POLICY_PROMPT = (
    "Политика: SQL выполняется только через MCP Postgres (read-only: SELECT/WITH/EXPLAIN, "
    "один statement). DSN и секреты скрыты от тебя — указывай только алиас target из списка. "
    "Не предлагай INSERT/UPDATE/DELETE/DROP и прочие изменяющие команды."
)
